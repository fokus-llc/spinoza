
#fl-navigation

  %nav.fl-authentication
    %ul.fg-actions
      - if user_signed_in?
        %li= link_to "log out: #{current_user.username}",
          destroy_user_session_path, :method=>'delete'
      - else
        %li= link_to "log in", new_user_session_path


